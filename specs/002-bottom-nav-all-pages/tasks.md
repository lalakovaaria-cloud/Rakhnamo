# Tasks: Bottom Nav All Pages

**Branch**: `002-bottom-nav-all-pages`

## Tasks

- [x] T001 Add bottom nav + `pb-24` to `application/app/documents/page.tsx` — active icon: documents (last icon)
- [x] T002 Add bottom nav + `pb-10` to `application/app/roadmap/page.tsx` — no active icon (all muted)
- [x] T003 Verify build passes (`npm run build` in `application/`)
